import { PlaceOrderSectionProps } from './types';
import styles from './PlaceOrderSection.module.css';

export const PlaceOrderSection = ({}: PlaceOrderSectionProps) => {
  return <section className={styles.section}></section>;
};
